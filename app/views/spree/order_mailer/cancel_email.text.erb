<%= t('order_mailer.cancel_email.dear_customer') %>

<%= t('order_mailer.cancel_email.instructions') %>

============================================================
<%= t('order_mailer.cancel_email.order_summary_canceled') %>
============================================================
<% @order.line_items.each do |item| %>
  <%= item.variant.sku %> <%= raw(item.variant.product.name) %> <%= raw(item.variant.options_text) -%> (<%=item.quantity%>) @ <%= item.variant.display_amount %> = <%= item.display_amount %>
<% end %>
============================================================
Részösszeg:: <%= @order.display_item_total %>
<% @order.adjustments.eligible.each do |adjustment| %>
  <%= raw(adjustment.label) %> <%= adjustment.display_amount %>
<% end %>
Összesen: <%= @order.display_total %>

Ha nem az Ön kérésére vontuk vissza megrendelést, vagy egyéb észrevétele van, kérjük lépjen velünk kapcsolatba.

Picur Webáruház